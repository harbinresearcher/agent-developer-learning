# Obsidian 界面/运行状态：每次开关应用都会重写，多设备同时备份必然冲突
# 注意必须带前导点，匹配 .obsidian 目录（旧规则漏了点，导致一直没生效）
**/.obsidian/workspace.json
**/.obsidian/workspace-mobile.json
**/.obsidian/cache

# 插件密钥，绝不入库
**/api-keys.json

# LearnKit 学习数据库：SQLite 二进制，两台设备同时写必然冲突且无法合并。
# 用户只在电脑端使用 LearnKit，故这些文件保持本地专属、不参与同步。
**/learnkit/scheduling/*.db
**/LearnKit/*.db

# obsidian-git 自动生成的凭据辅助脚本，每台设备各自生成，无需同步
**/obsidian_askpass.sh
